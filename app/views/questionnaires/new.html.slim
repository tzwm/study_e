h1 创建问卷
h2 = t("phase.#{@phase}")

button#addQuestion.btn.btn-primary type="button" data-toggle="modal" data-target="#addQuestionModal" 添加问题

= form_tag lesson_questionnaires_path, method: :post, id: "questionnaireForm" do
  input type="hidden" name="phase" value=@phase
  
  = submit_tag "下一步", class: "btn btn-default"

= render 'add_question_modal'
